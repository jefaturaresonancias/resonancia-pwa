// js/views/bot.js — Vista Bots (panel de Railway embebido vía iframe en
// index.html). No hay nada que cargar por JS acá — el iframe trae su
// propio contenido de forma independiente.
const BotView = (() => {
  function init() {}
  function cargar() {}
  return { init, cargar };
})();
